The simple mysql, postgres sandbox for my local development

### Up
setup all database
```sh
$ docker-compose up -d
```

### down
```sh
$ docker-compose down
```

### Only MySql
```sh
$ docker-compose up -d mysql
```

### Only Postgres
```sh
$ docker-compose up -d postgres
```
